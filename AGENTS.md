# Codex 작업 지침

이 저장소에서 작업하기 전에 루트의 `CLAUDE.md`를 먼저 읽고 해당 지침을 따릅니다.

- `frontend/` 작업 시 `frontend/CLAUDE.md`도 함께 확인합니다.
- `backend/` 작업 시 `backend/CLAUDE.md`도 함께 확인합니다.
- 공통 요구사항, API 계약, Git·REST 규칙 및 DB 스키마는 `docs/`의 문서를 기준으로 합니다.
- 지침이 서로 충돌하면 더 구체적인 하위 디렉터리 지침을 우선하되, 요구사항과 API 계약의 변경이 필요한 경우 관련 문서의 정합성을 함께 확인합니다.

