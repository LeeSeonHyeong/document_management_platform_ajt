# 백엔드 공개 API base URL
# 개발: Vite 프록시(/api → localhost:8080)를 쓰므로 상대경로 그대로 사용
# 배포: 실제 백엔드 오리진을 포함해 지정 (예: https://api.ajt.com/api/v1)
VITE_API_BASE_URL=/api/v1

# 목 API(MSW) 사용 여부. 백엔드 없이 화면을 개발/시연할 때 true.
VITE_ENABLE_MSW=false
