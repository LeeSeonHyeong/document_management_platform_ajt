# 2026-07-27-opus46-12docs

본 측정. 문서 12건을 1건씩 순서대로 처리했다 (FR-AI-003).

- 런타임 claude-code · 모델 claude-opus-4-6 · 범위 ALL
- 코퍼스 `spikes/wiki-convert-loop/corpus/` 12건 (PostHog 핸드북)
- 95분 · $25.37 · 툴 439회 · 턴 408 · 실패 0

## 무엇을 재려 했나

이전 spike(`spikes/wiki-convert-loop`)가 12건 규모에서 무너진 세 가지가 이 구조에서도
무너지는지 본다 — 조각 남발, 카테고리 붕괴, merge 미발동. 그리고 위키가 쌓일 때 시간이
어떻게 변하는지.

## 결과

| | 이전 spike (v2, sonnet-5, 12건) | 이 측정 |
|---|---|---|
| 최종 페이지 | 26 | **20** |
| 변경 유형 | create 26 · update 11 | **update 32 · create 20** |
| 조각(<1500자) | 13 / 26 | **0 / 20** |
| 카테고리 | 1개로 붕괴 | **5개** |
| 페이지간 링크 | 0 | **69** |
| 각주 | 페이지별 유무만 확인 | **616개** |
| 위치 있는 각주 | 미검증 | **616 / 616** |
| 원문 인용 각주 | 없던 개념 | **613 / 616** |
| 표 / mermaid | 22 / 13 | 20 / 19 (전 페이지) |
| lint error | — | **0** |

카테고리: `경비 정책` `근무 정책` `보안 정책` `복지 제도` `인사 제도`

툴 호출: `edit` 225 · `read` 83 · `search` 56 · `lint` 43 · `create` 20 · `guide` 12

## 판정

**통합이 실제로 일어난다.** update 32 > create 20. 이전 spike는 반대였다(create 26 : update 11).

**`merge` 툴은 0회.** 다만 이유가 다르다 — 겹치는 내용을 `edit`으로 흡수했다. 페이지가 흩어진
뒤 합쳐야 하는 상황이 생기지 않았다. 이전 spike의 merge 0은 "발췌로 겹침을 판단할 수 없어서"였다.

**근거가 기계 검증됐다.** 각주 616개 전부 위치가 있고 613개가 원문을 그대로 인용했으며, `lint`가
그것을 원본과 문자열 대조해 통과시켰다. FR-WIKI-001·NFR-AI-002가 12건 규모에서 검증된 상태다.
이전 spike의 미검증 항목 1번이 닫혔다.

**`lint` 43회 / 문서 12건 = 문서당 3.6회.** 지시가 "error 0까지"인데 평균 3.6번 돌았다 —
처음에 error를 냈고 고쳐서 통과시켰다는 뜻이다.

## 미해결

**NFR-PERF-002(문서당 10분) 초과 3건**: `09-spending-money` 14:13 · `11-offsites` 14:54 ·
`10-onboarding` 12:03.

시간은 문서 크기가 지배한다 (회귀: `1.9분 + 5.0분 × 크기/1만자`, R² 0.853). 즉 **10분 상한에서
실질 처리 한계가 약 1만6천자**다. (정정: 원판의 "문서당 2만 토큰 상한과 5배 어긋난다"는 요구사항에
없는 규정을 인용한 오기였다 — 실제 상한은 파일당 20MB. 이 측정을 근거로 크기 비례 상한(최대
30분)이 합의됐고 `runtime/limits.py`가 구현이다.) 한국어 실문서는 같은 토큰에 바이트가 3배라
한계가 더 빨리 온다.

`stale-page` warn 14건은 작업 경계를 넘는 정상 신호다 — 뒤 문서가 고친 페이지를 앞 페이지가
링크하고 있으면 찍힌다. 확인해볼 만한 대상이라 끄지 않았다.
