# 2026-07-27-opus46-2docs-pre

C안 리팩토링(2단 저장 · `pageKey` 주소 · 카테고리 DB화 · 작업 공간) 직후 첫 e2e.

## 무엇을 재려 했나

배관이 도는지. 라이브/작업 2단 분리, 커밋 시 `wikiId` 발급, 파일명 불변, 변경안 반환.

## 결과

- 두 문서 모두 완주, lint error 0
- 새 레이아웃대로 떨어졌다: `pages/{pageKey}.md` 평면, 카테고리는 경로에 없음
- 커밋 후 `work/` 삭제됨, 파일명 안 바뀜 — C안의 핵심이 확인됐다
- 각주별 근거(`documentId`·`location`·`quote`)가 변경안에 실렸다 (FR-AI-009)
- 2번째 문서가 1번째가 만든 페이지를 `edit`으로 고쳤다

## 판정

구조는 동작. 대신 산출물 품질에서 버그 4개가 드러나 `2026-07-27-opus46-2docs` 로 이어졌다.

## ⚠ lint error 36건은 이 측정의 결과가 아니다

이 실험의 `index.db`는 **구 스키마**였다(`document_references`에 `scope_id` 없음). `rebuild.py`로
재구축했으나 **원본문서명 2건이 복구되지 않았다** — 파일명은 DB에만 있고 경로에 없다(DR-016).

그래서 `experiments.py`가 이 실험에 **lint error 36건**을 표시한다. 전부
`unresolved-citation`이고, 각주가 가리키는 원본문서명을 인덱스가 모르기 때문이다.
**재구축의 부산물이며 측정 당시에는 error 0이었다.**

유효한 지표: 페이지 수 · 조각 수 · 링크 수 · 각주 수(34개, 전부 원문 인용).
무효한 지표: lint error/warn · 인용 해결 여부.
