# 2026-07-27-opus46-2docs

버그 4개를 고친 뒤 문서 2건으로 재측정. 12건 본 측정의 직전 확인.

## 무엇을 재려 했나

`2026-07-27-opus46-2docs-pre` 에서 드러난 네 가지가 고쳐졌는지.

1. **인용문 잘림** — 아포스트로피를 인용 부호로 취급해 `"We don't think..."`가 `We don`으로 잘렸다
2. **각주 위치** — `edit`으로 상호 참조를 붙이면 각주 정의 블록 뒤에 산문이 왔다
3. **stale 오탐** — 같은 작업 안에서 쓴 페이지가 stale로 찍혔다
4. **관계 그래프** — 간선이 행 ID 기준이라, 커밋된 페이지를 다음 작업에서 고치면 역참조가
   전부 사라졌다. **쓰기 후 피드백 루프가 조용히 끊긴다**

## 결과

- 4:18 · $1.54 · lint **error 0 / warn 0**
- 각주 32개, **위치 32 / 32, 원문 인용 32 / 32**
- 조각 0 · 페이지간 링크 1 · 표 2 · mermaid 2

아포스트로피가 든 문장이 온전히 인용됐다:

```
[^3]: 02-side-gigs.md, Side gigs — "we also currently don't offer part time work as an option at PostHog"
```

두 페이지 모두 마지막 줄이 각주 정의다 — `footnotes-not-at-tail` 사라졌다.

## 판정

네 가지 모두 해결. 이 코드로 12건 본 측정에 들어갔고, 이후 `mcp/` 는 건드리지 않았다.

4번은 테스트가 잡았다 — "작업이 바뀌면 stale이 뜨는가" 테스트가 실패하면서 드러났다. 그래서
관계 그래프를 **주소 기준**으로 바꿨다 (`pageKey`가 페이지 수명 동안 안 바뀌는 것이 여기서
값을 한다).
