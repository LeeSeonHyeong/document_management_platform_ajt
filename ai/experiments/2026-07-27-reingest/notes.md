# 2026-07-27-reingest

`2026-07-27-opus46-12docs` 결과(20페이지)를 복사해 `01-training.md` 1건을 다시 투입했다.

## 무엇을 재려 했나

조건을 **하나만** 바꾼 대조. 같은 문서·같은 모델·같은 지시문, 위키만 0페이지 → 20페이지.

1. **위키 누적 비용** — 위키가 커지면 문서당 시간이 늘어나는가
2. **재처리 멱등성** (FR-DOC-012) — 이미 반영된 문서를 또 올리면 중복이 생기는가

## 결과

| | 최초 (위키 0페이지) | 재투입 (위키 20페이지) |
|---|---|---|
| 소요 | 2:06 | **1:17** |
| 툴 호출 | 13회 | 8회 |
| 비용 | $0.65 | $0.48 |
| 반영 | 3건 (create 2 · update 1) | **0건** |

툴 순서: `guide` 1 → `search` 2 → `read` 4 → `lint` 1. **쓰기 툴 0회.**

에이전트 응답:

> 원본문서 `01-training.md` 내용은 이미 [교육 훈련 예산](pages/75759a021a75.md)과
> [도서 지원 정책](pages/b63430958f86.md) 두 페이지에 빠짐없이 반영돼 있었고, index.md에도
> 등록 완료 상태. 추가 변경 불필요.

최초 처리에서 만든 두 페이지를 정확히 짚었다.

## 판정

**멱등성 통과.** `create` 0건. 검색 2번·읽기 4번으로 대조한 뒤 안 썼다 — 찍은 것이 아니다.
같은 문서를 다시 올려도 위키가 부풀지 않는다.

**위키 누적 비용은 측정 한계 안에서 0.** 위키가 0 → 20페이지로 늘었는데 오히려 빨랐다. 12건 런의
회귀식(`시간 = 1.9분 + 5.0분 × 크기/1만자`)에 위키 크기 항이 없는 것이 확인됐다.

이전 spike는 위키 전체를 프롬프트에 주입해 위키 1건당 325토큰이 쌓였다. 여기서는 20페이지 중
2페이지만 읽고 끝냈다 — 검색 방식이 그 비용을 없앤다.

**남는 문제**: 아무것도 바뀌지 않는 작업에 1분 17초·$0.48이 든다. 12건 재처리면 약 $6. 백엔드가
문서 해시로 미리 걸러내면 없앨 수 있다 — 지금은 그 판단이 에이전트에게 있다.

**이 실험이 버그를 하나 드러냈다**: `backend_sim`이 변경 0을 실패로 기록했다. 재처리에서 변경 0은
정상 결과다. `outcome`을 `committed`/`unchanged`/`failed`로 나눠 고쳤다.
