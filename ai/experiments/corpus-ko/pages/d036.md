---
title: 성능 평가 지표 4
category: 엔지니어링
tags: []
---

# 성능 평가 지표 4

응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.
응답 시간은 p50, p95, p99 로 평가한다. 평가 구간은 5분 단위 집계를 쓴다.
처리량 평가는 초당 요청 수를 기준으로 한다. 평가 결과가 목표를 벗어나면 알림을 발송한다.
부하 테스트 평가는 배포 전에 반드시 수행한다.