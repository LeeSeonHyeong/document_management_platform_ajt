---
title: API 옵션 설정 5
category: 엔지니어링
tags: []
---

# API 옵션 설정 5

요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.
요청 옵션은 헤더와 쿼리 파라미터로 전달한다. 옵션이 누락되면 기본값을 적용한다.
타임아웃 옵션의 기본값은 30초이며 최대 300초까지 지정할 수 있다.
재시도 옵션은 3회를 기본으로 하고 지수 백오프를 적용한다. 옵션 조합에 따라 응답이 달라지므로
문서에 표로 정리했다. 잘못된 옵션은 400 응답과 함께 필드 이름을 반환한다.