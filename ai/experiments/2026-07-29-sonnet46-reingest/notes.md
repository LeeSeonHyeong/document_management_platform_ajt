# 2026-07-29-sonnet46-reingest

- 런타임: claude-code · 모델: claude-sonnet-4-6
- 범위: ALL · 시작점: `2026-07-27-opus46-12docs` 의 data/ 복사 (20페이지 위키)
- 재투입 문서: `01-training.md` (이미 반영된 것)
- guide: b5444b72e5a4 (4549자)

## 무엇을 재려 했나

**20페이지 위키에서 `search` 가 기존 페이지를 찾아내는가.** 못 찾으면 이미 있는 내용을 새
페이지로 만든다 — 이전 spike 의 대표 실패 모드다.

기준선은 `2026-07-27-reingest` (opus, 같은 위키·같은 문서): 77.6초 · 툴 8회 · **반영 0건**.

12건 측정과 병렬로 돌렸다. 그래서 **시간은 오염됐다** — 판정 지표는 contention 에 면역인
반영 건수와 툴 구성이다.

## 결과

| 지표 | opus | sonnet | 판정 |
| --- | --- | --- | --- |
| **반영** | **0건** | **0건** | **통과** |
| 판정 | `unchanged` | `unchanged` | 통과 |
| 툴 합 | 8 | 7 | — |
| `guide` / `read` / `lint` | 1 / 4 / 1 | 1 / 4 / 1 | 같음 |
| **`search`** | **2** | **1** | sonnet 이 절반 |
| 비용 | $0.48 | $0.31 | 35% 저렴 |
| 시간 | 77.6초 | 54.8초 | **오염 — 판정에 쓰지 않는다** |

## 판정

**멱등성 통과 (FR-DOC-012).** sonnet 도 20페이지 위키에서 기존 페이지를 찾아내고 "이미
반영됐다"고 판단하고 아무것도 바꾸지 않았다. 이전 spike 의 중복 생성이 재현되지 않았다.

**`search` 를 절반만 부른다.** opus 2회, sonnet 1회. 이 규모에서는 1회로 충분했다. 다른
측정에서도 같은 방향이다 (2건 변환 opus 6 / sonnet 4).

## 이 실험이 재지 않은 것

**생성 품질이 아니다.** 아무것도 만들지 않는 것이 정답인 상황이었으므로 각주·표·링크를
검증할 대상이 없었다. "큰 위키에서 생성이 잘 되는가"는 `2026-07-29-sonnet46-12docs` 가
문서 8~12를 17페이지 위키에 넣으면서 부분적으로 잰다.

**프로덕션 조건이 아니다.** `LocalVaultFS` 로 돌아 **20페이지 전부가 색인된 상태**였다.
프로덕션은 1단계(`selection.py`)가 목차에서 고른 최대 5개만 색인한다. 따라서 이 결과의
정확한 진술은:

> 20페이지가 **전부 색인된 조건**에서 중복을 만들지 않는다.

프로덕션에서 같은 결론이 나오려면 1단계가 그 페이지를 골라야 한다. **1단계 선택 정확도는
미측정이다** (설계 §7 리스크).
